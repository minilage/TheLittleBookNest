<UserControl x:Class="TheLittleBookNest.View.BooksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.InputBindings>
        <KeyBinding Key="PageDown" Command="{Binding ScrollToBottomCommand}" />
    </UserControl.InputBindings>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- DataGrid för att visa böcker -->
        <DataGrid x:Name="BooksDataGrid"
                  Grid.Row="0"
                  ItemsSource="{Binding Books}"
                  AutoGenerateColumns="True"
                  Margin="10"
                  IsReadOnly="True" />

        <!-- Add Book Button längst ner -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,10">
            <Button Content="Add Book"
                    Command="{Binding AddBookCommand}"
                    Width="120"
                    Height="40"
                    Style="{StaticResource PrimaryButtonStyle}" />
        </StackPanel>
    </Grid>
</UserControl>
